- Effect: 'Allow'
  Action: 'ssm:GetParameters'
  Resource:
    - !Sub arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/fauna/resource-key-${
      self:custom.keyStage.${self:custom.stage}, self:custom.keyStage.other}
# Using default key from AWS, so all users should have permission already
